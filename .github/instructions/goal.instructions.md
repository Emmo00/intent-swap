---
applyTo: '**'
---
prompt: use Spend Permissions (from Base Account SDK) on the client side and CDP Server Wallets to build an onchain AI agent that is able to access users' funds. 

the ideal setup allows users to grant/revoke Spend Permissions to one or multiple CDP server wallets (EOA or Smart Account) which interacts on the users behalf.
